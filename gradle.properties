# Nexus Repository 설정 (로컬 테스트용)
nexusUrl=http://localhost:8081/repository
nexusUsername=admin
nexusPassword=admin123

# 운영환경에서는 환경변수 사용 (보안 권장)
# nexusUsername=${NEXUS_USERNAME}
# nexusPassword=${NEXUS_PASSWORD}

# Nexus Repository 이름들
nexusReleaseRepo=maven-releases
nexusSnapshotRepo=maven-snapshots
nexusProxyRepo=maven-proxy
nexusBootRepo=boot

# 프로젝트 설정
org.gradle.parallel=true
org.gradle.caching=true
